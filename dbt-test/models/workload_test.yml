version: 2

models:
    - name: workload
      columns:
        - name: campaign_id
          tests:
            - not_null

        - name: live_status
          tests:
            - accepted_values:
               values: 
                - Starting
                - Live
                - Ending  